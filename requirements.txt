# Core
langchain                   # Framework for building LLM pipelines
python-dotenv               # Manage environment variables and API keys
streamlit                   # Simple UI framework for RAG applications

# Document Ingestion
pypdf                       # PDF ingestion and chunking
python-docx                 # Extract text from Word (.docx) documents
openpyxl                    # Read/write Excel (.xlsx) spreadsheets
markdownify                 # Convert HTML content into Markdown
pandas                      # Data manipulation and tabular preprocessing
filetype                    # Detect and verify file formats reliably

# Embeddings / Vector Store / RAG
openai                      # Compute embeddings via OpenAI API
sentence-transformers       # Pre-trained models for local embedding (optional)
torch                       # Backend for sentence-transformers (oder tensorflow)
chromadb                    # Vector database for similarity search

# Automatisierung
watchdog                    # Monitor filesystem changes for automated re-indexing
apscheduler                 # Schedule periodic jobs (z. B. Re-Chunking, Sync-Tasks)

# Feintuning
datasets                    # Laden und Vorbereiten von Datensätzen für Fine-Tuning
accelerate                  # Optimiertes Training & verteiltes Fine-Tuning

# Utility
black                       # Code-Formatter für konsistenten Stil
flake8                      # Linter zur Durchsetzung von Code-Qualitätsstandards
